require 'nokogiri'
require 'open-uri'
require 'csv'

finally = []
browser = 'Mozilla/5.0 (Macintosh; Intel Mac OS X 10_8_4) AppleWebKit/536.30.1 (KHTML, like Gecko) Version/6.0.5 Safari/536.30.1'

url = "https://www.nwacsports.org/Letters_of_Intent"

page = URI.open(url, "User-Agent" => "windows Firefox")
parsed = Nokogiri::HTML(page).xpath('//table')

parsed.xpath("//tr").each do |node|
    row = []
    if node.inner_text.include?("Women\'s Basketball")
            row << node.inner_text.split(" ")
    end
        finally << row
end

clean = []

finally.each do |row|
    if !row.empty?
        clean << row
    end
end

headers = "" ###Add this later ...  maybe need to grab individual td's


CSV.open("nwac_womens_basketball_lois.csv", "wb") do |csv|
    clean.each do |subArray|
        csv << subArray
    end
end





